const hideButton = document.getElementById("hideButton");
const divHide = document.getElementById("videoSelector");

  hideButton.addEventListener('click', event => {
  divHide.style.display = 'none';
});